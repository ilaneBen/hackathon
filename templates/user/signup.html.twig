{% extends "base.html.twig" %}

{% block title %}S'inscrire{% endblock %}

{% block body %}
test
    <div {{ react_component('Signup') }}></div>

    {# {{ form_start(form, {'attr': {'class': 'row', 'id': 'user-form'}}) }}
        <h1 class="h3 font-weight-normal">S'inscrire</h1> #}

        {# <div class="form-group col-6">
            {{ form_label(form.firstName, 'Prénom', {'attr': {'class': 'form-label'}}) }}
            {{ form_widget(form.firstName, {'attr': {'class': 'form-control'}}) }}
        </div>
        
        <div class="form-group col-6">
            {{ form_label(form.name, 'Nom', {'attr': {'class': 'form-label'}}) }}
            {{ form_widget(form.name, {'attr': {'class': 'form-control'}}) }}
        </div>

        <div class="form-group col-12">
            {{ form_label(form.email, 'Email', {'attr': {'class': 'form-label'}}) }}
            {{ form_widget(form.email, {'attr': {'class': 'form-control'}}) }}
        </div>
        
        <div class="form-group col-12">
            {{ form_label(form.password, 'Mot de passe', {'attr': {'class': 'form-label'}}) }}
            {{ form_widget(form.password, {'attr': {'class': 'form-control'}}) }}
        </div>
        
        <div class="form-group col-12">
            <button type="submit" class="btn btn-primary">S'inscrire</button>
        </div> #}
    {# {{ form_end(form) }} #}
{% endblock %}